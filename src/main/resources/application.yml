spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/hkidb?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
        username: root
        password: 1234
    jackson:
        property-naming-strategy: SNAKE_CASE
# 쿼리 수행시 콘솔창에 출력되는 옵션 추가
#    jpa:
#        properties:
#            hibernate:
#                format_sql: true
#                show_sql: true

    output:
        ansi:
            enabled: always # 콘솔창 색상 적용



jwt:
    secret: "HKI_BACKTEST_2024_jwt_token_key_value_spring_boot_flutter_project" #secret 값